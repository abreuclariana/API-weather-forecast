<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather Forecast</title>

    <!-- Bootstrap 5 - CDN do Cloudflare -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">

    <!-- Estilos personalizados -->
        <style>
             *{
                margin: 0;
                padding: 0;
                box-sizing: border-box;
                font-family: "Open Sans", sans-serif;
            }

        body {
            background-image: url("https://picsum.photos/1920/1089?random=1");
            color: white;
            text-align: center;
            padding: 20px;
        }

        .container {
            max-width: 500px;
            background: #000000;
            opacity: 0.8;
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
        }

        input, button {
            margin-top: 10px;
        }

        .weather-card {
            margin-top: 20px;
            padding: 20px;
            border-radius: 10px;
            background: rgba(255, 255, 255, 0.3);
        }

        img {
            width: 100px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Weather Forecast</h1>

        <!-- Formulário para pesquisar cidade -->
        <form action="/" method="post">
            <div class="mb-3">
                <input type="text" name="cityName" class="form-control" placeholder="Enter the name of the city" required>
            </div>
            <button type="submit" class="btn btn-light">Search</button>
        </form>

        <!-- Exibir informações do clima apenas se uma cidade for pesquisada -->
        <% if (query) { %>
            <div class="weather-card mt-4">
                <h2 class="mb-3">Weather in <%= query %></h2>
                <h3><%= temperatura %>°C</h3>
                <p><%= descricao %></p>

                <% if (imgURL) { %>
                    <img src="<%= imgURL %>" alt="Ícone do clima">
                <% } %>
            </div>
        <% } %>
    </div>

    <!-- Bootstrap Script -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
